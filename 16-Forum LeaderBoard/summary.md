1.  `async` keyword to create an asynchronous function.

```javascript
const example = async () => {
  console.log("this is an example");
};
```

2. The `await` keyword waits for a promise to resolve and returns the result.
